name: flutter_application_1
description: A scalable Flutter project with clean architecture & BLoC.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  connectivity_plus: ^6.0.3
  cupertino_icons: ^1.0.8
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_test:
    sdk: flutter
  get_it: ^7.7.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.2.0
  integration_test:
    sdk: flutter
  path_provider: ^2.1.2
  shared_preferences: ^2.5.4

dev_dependencies:
  build_runner: ^2.4.8
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true